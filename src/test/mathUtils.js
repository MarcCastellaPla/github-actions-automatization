export const sumNumbers = (num1, num2) => {
  const result = num1 + num2;
  return result;
};

export const subtractNumbers = (num1, num2) => {
  const result = num1 - num2;
  return result;
};

export const multiplyNumbers = (num1, num2) => {
  const result = num1 * num2;
  return result;
};

export const divideNumbers = (num1, num2) => {
  const result = num1 / num2;
  return result;
};
